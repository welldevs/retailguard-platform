# DuckDB — local warehouse / raw layer
duckdb>=1.0.0

# dbt
dbt-core>=1.11.0
dbt-duckdb>=1.10.0
dbt-snowflake>=1.11.0

# Testing
pytest>=8.0

# Linting
ruff>=0.4.0

# Snowflake — RAW layer upload (batch CSV + streaming Parquet)
snowflake-connector-python>=3.0.0

# Streamlit — the Executive Decision Platform (app/, multipage) run locally over
# DuckDB, and the Snowflake-native dashboard (snowflake/streamlit_app.py).
# Note: when deployed *inside* Snowflake (Streamlit in Snowflake), the
# runtime provides streamlit + snowpark; this pin is only for running locally.
streamlit>=1.36.0            # 1.36+ required for st.navigation / st.Page (app/ multipage platform)
pandas>=2.0.0                # dataframes for the Streamlit panels (imported directly)

# Streaming pipeline: Kafka + MinIO + Parquet
kafka-python>=2.0.0          # Kafka producer / consumer client
pyarrow>=15.0.0              # Parquet I/O (write to MinIO, read in Snowflake)
minio>=7.2.0                 # MinIO Python client (S3-compatible)
lz4>=4.0.0                   # lz4 compression for kafka-python
python-dotenv>=1.0.0         # load .env credentials automatically
